#include <stdio.h>
#define PI 3.14159

int main() {
    float raio, area;

    printf("Digite o raio da circunferencia: ");
    scanf("%f", &raio);

    area = PI * raio * raio;

    printf("A area da circunferencia é: %.2f\n", area);

    return 0;
}